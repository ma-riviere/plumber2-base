# R artifacts
.Rhistory
.RData
.Rproj.user/
.Rapp.history

# renv machinery (keep renv.lock and renv/activate.R; ignore the rest)
**/renv/library/
**/renv/staging/
**/renv/local/
**/renv/cellar/
**/renv/sandbox/

# Secrets and env
.Renviron
*.env
# Local-stack DB credentials only, no secrets (see the file header)
!deploy/db.env

# Build output (fingerprinted assets, regenerated by scripts/build-assets.R)
front/dist/

# Scratch and caches
.superpowers/
cache/

# Framework spike: local-only reference (proofs + SPIKE-FINDINGS.md), untracked since v1 shipped
spike/

# OS noise
.DS_Store
Thumbs.db
*~
.directory

# Working documents (local only, never committed)
CLAUDE.md
TODO.md
NOTES.md
PLAN.md
